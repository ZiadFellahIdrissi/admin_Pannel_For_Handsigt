<%- include('../partials/head', { title: 'Settings' }) %>
<div class="flex min-h-screen">
  <%- include('../partials/sidebar') %>
  <div class="flex-1 flex flex-col min-w-0">
    <%- include('../partials/topbar', { title: 'Settings' }) %>
    <%- include('../partials/flash') %>
    <div class="max-w-4xl mx-auto px-3 sm:px-4 py-6 sm:py-8 w-full">
      <h1 class="text-lg sm:text-xl font-bold mb-6 flex items-center gap-3">
        <div class="icon-circle-accent"><i class="fa-solid fa-gear"></i></div>
        Settings
      </h1>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <a href="/settings/administrative-information" class="card-interactive p-5 flex items-center gap-4">
          <div class="icon-circle-accent"><i class="fa-solid fa-building-columns"></i></div>
          <div>
            <div class="font-semibold">Administrative Information</div>
            <div class="text-xs text-brand-gray2 mt-0.5">ICE, RC, Patente, IF, CNSS, bank details, invoice logo</div>
          </div>
        </a>
      </div>
    </div>
  </div>
</div>
<%- include('../partials/foot') %>
