<%- include('../partials/head', { title: 'Suppliers' }) %>
<div class="flex min-h-screen">
  <%- include('../partials/sidebar') %>
  <div class="flex-1 flex flex-col min-w-0">
    <%- include('../partials/topbar', { title: 'Suppliers' }) %>
    <%- include('../partials/flash') %>
    <div class="max-w-4xl mx-auto px-3 sm:px-4 py-6 sm:py-8 w-full">
  <div class="flex items-center justify-between flex-wrap gap-3 mb-4 sm:mb-6">
    <h1 class="text-lg sm:text-xl font-bold flex items-center gap-3">
      <div class="icon-circle-accent"><i class="fa-solid fa-industry"></i></div>
      Suppliers
    </h1>
    <a href="/suppliers/new" class="btn-primary">
      <i class="fa-solid fa-plus"></i>New Supplier
    </a>
  </div>

  <div class="flex items-center gap-2 mb-4 text-sm">
    <a href="/suppliers?active=1" class="btn-secondary <%= activeFilter === '1' ? '!border-brand-accent !text-brand-accent' : '' %>">Active</a>
    <a href="/suppliers?active=0" class="btn-secondary <%= activeFilter === '0' ? '!border-brand-accent !text-brand-accent' : '' %>">Inactive</a>
    <a href="/suppliers?active=all" class="btn-secondary <%= activeFilter === 'all' ? '!border-brand-accent !text-brand-accent' : '' %>">All</a>
  </div>

  <p class="text-xs text-brand-gray2 mb-4">A supplier linked to at least one invoice can only be deactivated, not deleted - this preserves that invoice's record.</p>

  <% if (suppliers.length === 0) { %>
    <div class="card p-8 text-center">
      <i class="fa-regular fa-folder-open text-2xl text-brand-gray2 mb-2"></i>
      <p class="text-sm text-brand-gray2">No suppliers found.</p>
    </div>
  <% } else { %>
    <div class="space-y-3">
      <% suppliers.forEach(function(sp) { %>
        <div class="card-interactive p-4 flex items-center justify-between gap-3 flex-wrap">
          <div class="min-w-0">
            <a href="/suppliers/<%= sp.id %>" class="font-semibold hover:text-brand-accent"><%= sp.legal_name %></a>
            <% if (sp.ice) { %>
              <div class="text-xs text-brand-gray2 mt-0.5">ICE: <%= sp.ice %></div>
            <% } %>
          </div>
          <div class="flex items-center gap-2 shrink-0">
            <span class="<%= sp.active ? 'badge-approved' : 'badge-draft' %>"><%= sp.active ? 'Active' : 'Inactive' %></span>
            <a href="/suppliers/<%= sp.id %>/edit" class="btn-secondary text-sm"><i class="fa-solid fa-pen"></i></a>
            <form action="/suppliers/<%= sp.id %>/toggle-active" method="POST">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <button type="submit" class="btn-secondary text-sm"><%= sp.active ? 'Deactivate' : 'Activate' %></button>
            </form>
            <form action="/suppliers/<%= sp.id %>/delete" method="POST" data-confirm="Delete &quot;<%= sp.legal_name %>&quot;? This can't be undone.">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <button type="submit" class="btn-danger text-sm"><i class="fa-solid fa-trash"></i></button>
            </form>
          </div>
        </div>
      <% }); %>
    </div>
  <% } %>
    </div>
  </div>
</div>
<script src="/js/confirm-submit.js?v=<%= assetVersion %>" defer></script>

<%- include('../partials/foot') %>
